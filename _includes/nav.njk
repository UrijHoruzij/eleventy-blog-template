<nav class="sidebar__navigation">
  <ul class="navigation">
    {% for post in collections.post %}
      <li class="navigation__item">
        <a href="{{ site.url }}{{ post.url }}" class="navigation__link {% if page.fileSlug == post.fileSlug %}
  navigation__link--activ
{% endif %}">
        {{ post.data.title }}
        </a>
        {# <a href="{{ post.url }}" class="navigation__link {% if page.fileSlug == post.fileSlug %}
  navigation__link--activ
{% endif %}">
        {{ post.data.title }}
        </a> #}
      </li>
    {% endfor %}
  </ul>
</nav> 
